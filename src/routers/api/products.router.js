/*import { Router } from "express";

const productsRouter = Router();

productsRouter.post('/',()=>{ server.post("/api/products", create) })
productsRouter.get('/',()=>{ lógica para leer todos })
productsRouter.get('/:pid',()=>{ lógica para leer uno })
productsRouter.put('/:pid',()=>{ lógica para actualizar uno })
productsRouter.delete('/:pid',()=>{ lógica para eliminar uno })


server.get("/api/products/", read);
server.get("/api/products/:pid", readOne);
server.post("/api/products", create);
server.put("/api/products/:pid", update);
server.delete("/api/products/:pid", destroy);



export default productsRouter*/
